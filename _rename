#compdef rename
#
# For my version (there are other, incompatible, implementations (RHEL
# has a very wacky implementation, not even Perl)).

local ret=1

_arguments \
  '-c[copy instead of rename]' \
  '-f[force changes]' \
  '-i[interactive changes]' \
  '-M[make directories if necessary]' \
  '-m[move instead of rename]' \
  '-n[dry run]' \
  '-v[verbose]' \
  && ret=0

return $ret
